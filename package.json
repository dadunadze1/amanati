{
  "name": "delivery-company-dispatcher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node backend/server.mjs",
    "check": "node --check backend/server.mjs && node -e \"const { readdirSync } = require('fs'); const { join } = require('path'); const { execFileSync } = require('child_process'); for (const file of readdirSync('frontend/js').filter((name) => name.endsWith('.js'))) execFileSync(process.execPath, ['--check', join('frontend/js', file)], { stdio: 'inherit' });\"",
    "check:functions": "node --check functions/index.js",
    "test:api": "node --test tests/*.test.mjs",
    "test:ui": "node tests/run-ui-tests.mjs",
    "test": "npm run test:api && npm run test:ui",
    "ci": "npm run check && npm run check:functions && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
